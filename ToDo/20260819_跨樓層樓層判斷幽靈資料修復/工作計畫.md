# 二廠跨樓層樓層判斷幽靈資料修復

## 建立日期
2026-08-19

## 狀態
**方向已確定，可開工。**

## 來源
2026-08-19 客訴：AGV 在 2F，SCP 建立 3F→1F 任務不執行，RCS 只見 3→1 任務、無 2F→3F 預調度。
完整 log 佐證與根因分析見同資料夾 `spec.md`，技術方案與被捨棄方案見 `plan.md`。

## 範圍
- ✅ ① 資料層：`UpdateAGVStatus` 同輪去重 + MapCode 連續 3 次確認
- ✅ ② 決策層：`CrossFloorManager` 樓層不可信時不派發
- ❌ ③ oMission 卡住看門狗 — 使用者 2026-08-19 決定本次不執行
- ❌ 電梯中介程式 `WebApplication1` — 非我方負責

## 已確定的設計決策（2026-08-19 使用者拍板）
| 項目 | 決策 |
|------|------|
| 更新方式 | **保留原本輪詢寫入**，在寫 DB 前加裁決層（非改為 callback 單一來源） |
| 連續確認次數 | **N = 3**，寫成程式常數，不放設定檔 |
| 衝突處理 | **只記 WARN log**，不上報 FHt |
| 幽靈判準 | **座標是否變動**；**明令禁用 `robotIp`**（mock 硬寫 ""，且 8/19 有兩筆皆帶 IP 的區間） |

## 執行順序
P0-1（裁決器 + 測試）→ P0-2（接線）→ P0-3（決策層閘門 + 測試）→ 🚦驗證點 → P1-4（回歸）

## 單一任務完成 4 道 Gate

每個子任務完成、標記 `[x]` 之前必須全部通過：

| Gate | 檢查項 | 判定 |
|------|--------|------|
| 1. Build | 對應專案建置 | 0 error（warning 允許） |
| 2. Test | TDD: 對應測試 / 一般: 回歸測試 | 全部通過 |
| 3. Acceptance | 逐項核對 spec.md 對應驗收標準 | 全部滿足 |
| 4. Regression | 若屬重構 / 效能優化 | 完整測試套件 0 失敗 |

任一 Gate 失敗 → 標 `[!] 阻塞中` + 停下找使用者，不得跳過。

## 跨優先順序驗證點

### 🚦 驗證點 #1：P0 → P1（在進入第一個 P1 任務前執行）

完成的 P0 任務：P0-1、P0-2、P0-3

驗證步驟：
1. 重新執行 P0 任務新增的全部測試（`ShuttleMapCodeResolverTests`、`CrossFloorManagerFloorTrustTests`）
2. 重新執行既有測試 + 新增測試 → 0 失敗
3. 抽查 spec.md 的 AC1~AC11 全部滿足
4. 整合行為抽查：確認 `UpdateAGVStatus` 已無「foreach 內直接 `Update_oShuttle`」的殘留路徑
   （避免只改了一半，仍有旁路繞過裁決器）

通過 → 進入 P1 任務
失敗 → 停下找使用者討論修復方案，修復後再進入 P1

## 工作拆解計畫

- [x] [P0] 🧪 **P0-1** 新增 `ShuttleMapCodeResolver` 純裁決類別
  ├─ ✅ Gate1 Build PASS(0 error) / Gate2 Test 12 passed 0 failed / Gate3 AC1~AC8 具名對應全滿足
  ├─ 黏著順序定為「待確認值 → 認可值 → 上一輪勝出值」：直接黏認可值會在車靜止時把確認進度歸零，永遠翻不過去
  ├─ `Winner`(原始回報，診斷用) 與 `DbRow`(要寫 DB，mapCode 已裁決) 分離，避免確認窗口出現 MapCode 與座標分屬不同地圖的混搭列
  └─ 已知取捨：三種黏著目標本輪皆未回報時維持舊值且不推進計數，長期持續會停在舊值（每輪記 WARN 可追；自動脫困需第③項看門狗，本次不做）
  ├─ 路徑：`ACC/HikAGVWebAPI/HikAGVWebAPI/App_Start/ShuttleMapCodeResolver.cs`（UTF-8 BOM）
  ├─ 比照既有 `CooldownTracker` / `ElevatorPathCalculator` 慣例：無 SQLData / HikAGV / Log 依賴
  ├─ TDD 原因：純函數＋明確輸入輸出（本輪回報 + DB 現值 → 裁決後單筆），且為 bug 修復需回歸防護
  ├─ 對應 AC：AC1~AC8
  ├─ 測試分類：Happy Path / Edge Case / Boundary / Regression(實戰回放)
  └─ 預估測試案例數：10~12

- [x] [P0] **P0-2** `Dispatch.UpdateAGVStatus` 接線至裁決器
  ├─ ✅ Gate1 Build PASS(0 error) / Gate2 全部 121 測試通過(109 既有 + 12 新增) / Gate4 回歸 0 失敗
  ├─ 順帶修掉既有的稼動率重複計數：三個副作用原本每筆回報各跑一次，同車被兩張圖回報時一輪跑兩次
  └─ 新增 per-robot try/catch：改為收集後統一寫入後，單台車例外會讓整輪所有車都沒更新（本次重構自身引入的風險）
  ├─ 路徑：`ACC/HikAGVWebAPI/HikAGVWebAPI/App_Start/Dispatch.cs:163-241`
  ├─ 改「查一筆寫一筆」為「整輪收集 → Select_oShuttle 取現值 → Resolve → 寫入」
  ├─ `HasConflict` 時 `mLog.TraceOut(..., Log.LogType.WARN)`
  ├─ 稼動率（`InsertShuttleChargeActivate` / `InsertAbnormalActivate`）與 `CheckBattery` 改以裁決後單筆呼叫，避免同一輪重複計數
  └─ 非 TDD：與 SQLData / HikAGV / config 深度耦合，難以單元測試；以 Build + 整合抽查驗證

- [x] [P0] 🧪 **P0-3** `CrossFloorManager` 樓層可信度閘門
  ├─ ✅ Gate1 Build PASS(0 error) / Gate2+Gate4 全部 131 測試通過 / Gate3 AC9~AC11 滿足
  ├─ 抽出 `SelectNextMissionForFloor(pending, currentFloor, mapCodeForLog)` 作測試接縫：
  │   既有測試以 `mDB: null` 建構，直接測 `SelectNextMission` 會因 NRE 被 try/catch 吞掉而回 null，斷言假性通過
  ├─ 修正既有缺陷：`currentFloor` 與 `GetFloor(BeginStation)` 同為 null 時 `null == null` 誤判同樓層
  └─ ⚠️ 未嚴格走 Red→Green（接縫與實作一起完成才寫測試），改以 4 個 regression 案例確保既有行為不變
  ├─ 路徑：`ACC/HikAGVWebAPI/HikAGVWebAPI/App_Start/CrossFloorManager.cs:282-287, 315-350`
  ├─ 修正 `DecideNextCrossFloorAction` 的 `null == null` 誤判為同樓層（:333-335）
  ├─ `SelectNextMission` 取不到可信樓層 → return null；**系統任務分支（:273-276）維持在最前面不受影響**
  ├─ TDD 原因：核心決策邏輯，且 `DecideNextCrossFloorAction` 已是 public static 純函數，既有測試檔可直接擴充
  ├─ 對應 AC：AC9~AC11
  ├─ 測試分類：Edge Case / Error / Regression
  └─ 預估測試案例數：4~6

- [ ] [P1] **P1-4** 完整回歸驗證
  ├─ 不帶 filter 跑完 `HikAGVWebAPITests` 全部測試
  ├─ 對應 AC：AC12
  └─ 非 TDD：驗證性任務

## 測試案例矩陣

### P0-1 `ShuttleMapCodeResolverTests`

| # | 案例名稱 | 分類 | 輸入摘要 | 預期結果 | 優先序 | AC |
|---|---------|------|---------|---------|--------|-----|
| 1 | 單筆回報直接採用 | Happy Path | reports=[BB] | winner=BB, HasConflict=false | P0 | AC1 |
| 2 | 多筆取座標有變動者 | Happy Path | BB座標變動, DD凍結 | winner=BB, HasConflict=true | P0 | AC2 |
| 3 | 多筆皆凍結時黏著 | Edge Case | BB/DD皆凍結, accepted=BB | winner=BB | P0 | AC3 |
| 4 | 多筆皆變動時黏著 | Edge Case | BB/DD皆變動, accepted=BB | winner=BB | P0 | AC3 |
| 5 | 新出現的地圖視為變動 | Edge Case | BB首次出現, DD凍結 | winner=BB | P0 | AC2 |
| 6 | 變更需連續3次確認 | Boundary | accepted=DD, 連3輪勝出BB | 第1/2輪輸出DD, 第3輪輸出BB | P0 | AC4 |
| 7 | 確認中斷則重新計數 | Boundary | pending=BB(2次)後勝出FF | pending=FF(1次), 輸出DD | P0 | AC5 |
| 8 | callback 寫入後同步 | Edge Case | dbMapCode≠accepted | accepted=dbMapCode, pending清空 | P0 | AC6 |
| 9 | 8/19 實戰序列回放 | Regression | 11:29:04~11:29:24 實際回報 | 11:29:08起輸出BB, 11:29:24輸出BB | P0 | AC7 |
| 10 | 衝突描述含明細 | Happy Path | 多筆回報 | 描述含各筆mapCode/座標/勝出原因 | P1 | AC8 |
| 11 | 黏著目標本輪未回報 | Error | accepted=FF, 本輪只有BB/DD | MapCode維持FF不變更 | P1 | AC3 |
| 12 | 空回報清單 | Error | reports=[] | 回傳 null, 不拋例外 | P1 | — |

### P0-3 `CrossFloorManagerFloorTrustTests`

| # | 案例名稱 | 分類 | 輸入摘要 | 預期結果 | 優先序 | AC |
|---|---------|------|---------|---------|--------|-----|
| 1 | 樓層與站點皆null不得判同樓層 | Error | currentFloor=null, BeginStation="Z9" | 不回傳 SameFloor | P0 | AC9 |
| 2 | 樓層null但站點已知 | Error | currentFloor=null, BeginStation="J1" | 不回傳 SameFloor | P0 | AC9 |
| 3 | MapCode不可信不派發 | Edge Case | MapCode不在對照表 | SelectNextMission 回 null | P0 | AC10 |
| 4 | MapCode不可信不產生預調度 | Edge Case | 同上 | 無 CROSS_FLOOR_DISPATCH 寫入 | P0 | AC10 |
| 5 | 系統任務不受樓層可信度影響 | Happy Path | pending含CROSS_FLOOR_DISPATCH | 仍回傳該系統任務 | P0 | AC11 |
| 6 | 樓層可信時行為不變 | Regression | currentFloor="2F", BeginStation="H3" | 回傳 SameFloor(H3→K1) | P0 | AC9 |

## Commit 策略
每個子任務獨立 commit，格式：
- `fix(acc): UpdateAGVStatus 同輪去重與MapCode連續確認防幽靈資料覆蓋`
- `fix(acc): CrossFloorManager 樓層不可信時不派發跨樓層任務`
- `test(acc): 補 ShuttleMapCodeResolver 與樓層可信度單元測試`
