# 工作計畫：手冊補「預調度／自動回 4F 說明」＋「兩組 UI 截圖」＋「UML/Schema 更新」

## 主題與背景

客戶操作手冊「**項目二 智慧物流整合平台 使用說明書**」（手冊段落 [42]）目前有多項內容過時或缺漏，需補齊／更新。分三類：

**A. 系統自動行為文字說明**（接在 6 段樓層流程說明之後，當第 7、8 段）：
1. **預調度**（跨樓層預派調度）— 系統提前把跨樓層任務排給穿梭車的自動機制
2. **AGV 自動回 4F**（閒置自動復歸）— AGV 閒置逾時自動開回 4F 待命

**B. UI 截圖操作說明**（補進「網頁操作介面說明」[103] 區，比照該區「小標＋截圖」格式）：
3. **建立物料（物料登記）操作** — 掛在「任務派送介面」[109] 之後，新增小節「任務派送介面－物料登記（建立物料）」。圖在 `IMAGE\建立物料步驟\1~4.jpg`（M 區範例：①進任務派送頁 ②點 M1 站點彈出站點操作 ③物料登記輸工單條碼 LOT0001／可勾 V Cut 專用 ④儲存後站點轉綠）。**M 區只是範例，須註明其餘站點操作相同**。
4. **路線權限管理** — 補進「權限管理」家族，掛在 [142]「權限管理-用戶資料維護」之後，新增「權限管理-路線權限管理」。圖在 `IMAGE\路線權限管理\1~2.jpg`（①權限管理→路線權限管理、選使用者＋勾派送路線清單 ②勾「2F 出貨區(Q)→清洗區(S)」後儲存）。

**C. UML／TableSchema 更新**（更新「作業流程說明」[204] → 「各程式UML架構圖」[221] 底下的 4 張舊圖；舊圖已抽出存 `手冊更新素材\舊版UML與TableSchema\`）：
5. **三張 UML 圖全部過時，需重畫替換**（已比對實際程式碼確認缺口）：
   - Dispatch UML（[223]）：缺空平板回收 `CheckAndHandleEmptyPlateRecovery`／`ProcessSingleoNeed`；frmMain 仍是舊版 block 按鈕概念
   - HikWebApi UML（[226]）：**缺整個跨樓層調度核心** `CrossFloorManager`／`ElevatorPathCalculator`／`CooldownTracker`／`CrossFloorDecision`（二廠最大新增）
   - GUI UML（[234]）：`DispatchController` 缺 `Release`／`RegisterLot`／`ClearLot`／`MarkEmptyTray`／`DeleteoNeed`；缺 `RouteController`／`PortBindingController`／`TaskTypeRouteController` 三個新 Controller
   - 更新素材：`Doc/驗收文件/系統UML.md` 的 mermaid 類別圖已含上述新類別，渲染 png 替換即可（毋須重畫）
6. **TableSchema 更新（一定要做）**：舊圖是內嵌 Excel（`舊_TableSchema.xlsx`，僅 12 張基礎表，與 `agvDB_1400004.sql` 一致）。新功能多 5 張表，舊圖全缺：
   - `pRoute`（路線定義）、`pUserRoute`（使用者路線權限，對應 B 的路線權限管理 UI）、`oPortBinding`（上下料區綁定）、`oTaskTypeRoute`（TaskType 路由）、`ubCancelLog`（取消連動 log）
   - 欄位變更待逐一讀 Deploy SQL 確認：`oShuttle.UpdateTime`、`oNeed/oRequire.ParentTaskDateTime`、`oMission` 的 TaskSource/TaskType 等

- 手冊檔：`C:\Users\user\格路科技\格路科技 - 開發團隊 - 文件\project-客製化專案\2025\0.評估中\20251013_迅得_高技_自動搬運專案系統\11. 使用者操作手冊\高技二廠操作手冊v2.docx`
- 圖檔目錄：同手冊資料夾的 `IMAGE\建立物料步驟\`（1~4.jpg）、`IMAGE\路線權限管理\`（1~2.jpg）。
- 手冊現況（2026-06-08 實查）：267 段。項目二 [42] 底下＝6 段樓層流程說明（[43]一樓/[55]二樓站內/[68]二樓站外/[77]三樓插針/[86]三樓品檢/[95]四樓烘烤）→ [103] 網頁操作介面說明（框架/任務派送介面[109]/車輛稼動率/歷史任務/班別/權限管理-群組代碼[137]/群組功能[139]/用戶資料[142]/系統設定）→ [169] 平板操作介面說明。
- A 類兩段文字接在 [95] 四樓烘烤之後、[103] 網頁操作介面說明之前。
- UML 區位置：[204] 作業流程說明 → [221] 各程式UML架構圖（Dispatch UML[223] / HikWebApi UML[226] / GUI UML[234] / TableSchema[239] 內嵌 Excel）。
- **所有手冊相關資料集中放在手冊資料夾**：原圖在 `IMAGE\`，舊 UML/Schema 素材在 `手冊更新素材\舊版UML與TableSchema\`，後續產出（新 UML png、新 schema）也一律放手冊資料夾，不放 repo。

## 需求範圍

- **包含（A 文字）**：補「預調度」「自動回 4F」兩段純文字說明，格式比照既有流程說明（標楷體、Normal、小標粗體、純文字、技術代碼改白話）。
- **包含（B 截圖）**：補「建立物料（物料登記）」「路線權限管理」兩個 UI 小節，比照「網頁操作介面說明」區既有格式（小標＋置中截圖＋簡短步驟），圖用 IMAGE 目錄現成 jpg。
- **包含（C UML/Schema）**：替換 [221] 的 3 張 UML 舊圖（用 系統UML.md 渲染）；更新 [239] TableSchema（補 5 張新表＋欄位變更，沿用內嵌 Excel 形式或改 mermaid ER）。
- **不包含**：程式碼變更（功能皆已實作完成並歸檔，見 `ToDo/archive/20260320_預調度機制`、`20260320_AGV自動歸位機制`、`20260421_修復跨樓層預調度重複派發`、`20260320_TaskType資料庫遷移`）；不畫流程圖（A 純文字、B 用現成截圖）。

## 限制條件

- **內容須以實際程式碼核對為準**（鐵律，見記憶 feedback_dispatch_logic_read_code）。對應程式碼：
  - 預調度：`ACC/HikAGVWebAPI/HikAGVWebAPI/App_Start/CrossFloorManager.cs` 的 `SelectNextMission`/`DecideNextCrossFloorAction`（`CROSS_FLOOR_DISPATCH`）、`CooldownTracker`
  - 自動回 4F：同檔 `Tick()`（`IDLE_RETURN`）；設定 `HikAGV.config` 的 `IdleReturnTimeout`、`IdleReturnFloor`、`CrossFloorShuttleId`
- **編碼**：python-docx 插入；新建腳本/檔案一律 UTF-8 BOM。**編輯前確認 Word 已關閉**（會鎖檔）。
- **格式禁忌**：勿用 ●／灰斜體／縮排／多重粗體（先前被使用者打回）。
- `Doc/驗收文件/build/` 腳本內 DOCX 路徑仍指**舊位置**，重跑前先改成新路徑（`11. 使用者操作手冊\`）。

## 驗收標準

- [ ] 「預調度」段落已寫入手冊，內容與 `CrossFloorManager.cs` 實際行為一致，文字通順
- [ ] 「自動回 4F」段落已寫入手冊，內容與實際設定/程式碼一致，文字通順
- [ ] A 兩段位置正確（接在 [95] 四樓烘烤段之後、「網頁操作介面說明」之前），格式比照既有流程說明（標楷體、小標粗體、純文字）
- [ ] 「物料登記（建立物料）」小節含 1~4.jpg 與步驟文字，掛在「任務派送介面」[109] 之後，並註明「以 M 區為例，其餘站點相同」
- [ ] 「路線權限管理」小節含 1~2.jpg 與步驟文字，掛在「權限管理-用戶資料維護」[142] 之後，格式比照同區其他「權限管理-XXX」小節
- [ ] [221] 三張 UML 圖已替換為含新類別的新圖（Dispatch 含空板回收／HikWebApi 含跨樓層核心／GUI 含三個新 Controller 與 DispatchController 新方法）
- [ ] [239] TableSchema 已更新，含新增 5 張表（pRoute／pUserRoute／oPortBinding／oTaskTypeRoute／ubCancelLog）及確認過的欄位變更
- [ ] 原手冊內容無損（既有 6 段流程、9 個網頁介面小節、平板說明、作業流程說明其餘內容皆不受影響）

## 執行順序

建議 A → B → C（A 純文字最單純，C 牽涉渲染與 schema 較重）。各類內部皆為：讀程式碼/SQL 核對 → 產內容（文字／截圖／渲染圖）→ python-docx 插入/替換 → 驗證。

## 單一任務完成 4 道 Gate

每個子任務完成、標記 `[x]` 之前必須全部通過：

| Gate | 檢查項 | 判定 |
|------|--------|------|
| 1. Build | 對應專案建置 | 0 error（warning 允許） |
| 2. Test | TDD: 對應測試 / 一般: 回歸測試 | 全部通過 |
| 3. Acceptance | 逐項核對 spec.md 對應驗收標準 | 全部滿足 |
| 4. Regression | 若屬重構 / 效能優化 | 完整測試套件 0 失敗 |

任一 Gate 失敗 → 標 `[!] 阻塞中` + 停下找使用者，不得跳過。

> 註：本任務為**純文件撰寫**，無程式碼變更，Gate 1/2/4 不適用（記為 N/A）；驗收以 Gate 3（核對上述驗收標準）為主，並以「內容與程式碼一致」為硬性條件。

## 跨優先順序驗證點

本次計畫僅含 {P2} 任務，無跨優先序驗證需求。

## 工作項目

### A. 系統自動行為文字說明（接 [95] 之後）

- [x] [P2] 讀程式碼核對「預調度」與「自動回 4F」實際行為
  ├─ 非 TDD：探索性閱讀，無輸入輸出規格
  ├─ 核對 `CrossFloorManager.cs`（CROSS_FLOOR_DISPATCH / IDLE_RETURN / CooldownTracker）與 `HikAGV.config`（IdleReturnTimeout/IdleReturnFloor）
  └─ 結論：IdleReturnTimeout=300s/IdleReturnFloor=4F/CrossFloorShuttleId=3；歸位 Tick() cs:111-189；預調度 SelectNextMission/DecideNextCrossFloorAction cs:268-350；同樓層優先、30s 冷卻、預調度不寫 oRequire 靠 ParentTaskDateTime 連動
- [x] [P2] 撰寫「預調度」段落（純文字、比照既有格式，技術代碼改白話）
  └─ 非 TDD：文件撰寫；使用者確認措辭後定稿
- [x] [P2] 撰寫「AGV 自動回 4F」段落（純文字、比照既有格式）
  └─ 非 TDD：文件撰寫；使用者確認措辭後定稿
- [x] [P2] python-docx 插入 A 兩段（[95] 之後）並驗證位置、格式、原內容無損
  └─ 非 TDD：已插入 v2.docx，預調度[104]/自動回4F[111]，皆標楷體粗體小標；段落 267→283；插入前備份 `_備份_20260608_插A段前.docx`

### B. UI 截圖操作說明（補進「網頁操作介面說明」區）

- [x] [P2] 撰寫「物料登記（建立物料）」小節步驟文字（對 1~4.jpg，註明 M 區為例）
  └─ 非 TDD：文件撰寫；使用者確認措辭後定稿；小標非粗體（比照網頁操作介面說明區）
- [x] [P2] 撰寫「路線權限管理」小節步驟文字（對 1~2.jpg）
  └─ 非 TDD：文件撰寫；使用者確認措辭後定稿
- [x] [P2] python-docx 插入 B 兩小節（含 jpg 置中內嵌）並驗證位置、圖片正常、原內容無損
  └─ 已插入 v2.docx：建立物料[131]接任務派送介面後、路線權限管理[175]接用戶資料維護後；步驟文字＋對應圖交錯置中；段落 283→303、圖片 30→36；插入前備份 `_備份_20260608_插B段前.docx`

### C. UML／TableSchema 更新（更新 [221] 各程式UML架構圖）

- [ ] [P2] 讀程式碼確認 3 張 UML 缺口已涵蓋在 `系統UML.md`，必要處補成員（Dispatch/HikWebApi/GUI）
  └─ 非 TDD：核對；HikWebApi 重點是跨樓層核心、GUI 重點是 3 個新 Controller
- [ ] [P2] 用本機 Mermaid 工具鏈渲染 3 張新 UML png（Dispatch／HikWebApi／GUI），存手冊資料夾
  └─ 非 TDD：圖檔產生；工具鏈見 [[project_operation_manual_v2_progress]]（Chrome + mermaid-cli -e png，無外傳）
- [ ] [P2] 讀 Deploy SQL 確認新增 5 表欄位＋既有表欄位變更，整理新版 TableSchema
  ├─ 非 TDD：讀 `Deploy_pRoute/pUserRoute/oPortBinding/oTaskTypeRoute/ubCancelLog.sql` 與 `Deploy_*AddUpdateTime/ParentTaskDateTime.sql`
  └─ 沿用內嵌 Excel（更新 `舊_TableSchema.xlsx`）或改 mermaid ER（待動工時決定）
- [ ] [P2] python-docx 替換 [221] 4 張舊圖（3 UML + TableSchema）並驗證位置、圖片正常、原內容無損
  └─ 非 TDD：文件操作 + 驗證

## 備註

- 相關記憶：[[project_operation_manual_v2_progress]]（手冊進度與格式規範、新路徑）、[[feedback_dispatch_logic_read_code]]（讀 code 為準）、[[project_config_files_layout]]（HikAGV.config 設定位置）、[[project_system_architecture]]（跨樓層流程）。
- 建立日期：2026-06-08
