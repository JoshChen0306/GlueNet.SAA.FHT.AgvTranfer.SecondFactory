# 工作計畫 — 清除物料改標空板（HaveFlag 0→1）

> 建立日期：2026-06-02
> 規格見 spec.md
> 分析力度：精簡模式（單點字面值改動 + 人工驗證）

## 執行順序

單一改動：`DispatchController.ClearLot` 的 `HaveFlag "0"→"1"`，補註解說明語意。
測試採人工驗證（SCP 無測試專案，使用者裁定），不建測試基建。

## 單一任務完成 4 道 Gate

每個子任務完成、標記 `[x]` 之前必須全部通過：

| Gate | 檢查項 | 判定 |
|------|--------|------|
| 1. Build | 對應專案建置 | 0 error（warning 允許） |
| 2. Test | TDD: 對應測試 / 一般: 回歸測試 | 全部通過 |
| 3. Acceptance | 逐項核對 spec.md 對應驗收標準 | 全部滿足 |
| 4. Regression | 若屬重構 / 效能優化 | 完整測試套件 0 失敗 |

任一 Gate 失敗 → 標 `[!] 阻塞中` + 停下找使用者，不得跳過。

> 本案 Gate 2 無自動化測試可跑（SCP 無測試專案，使用者裁定採人工驗證），
> 以下方「Gate 2／4 人工驗證清單」替代，並於完成報告中註明「採人工驗證 fallback」。

## 跨優先順序驗證點

本次計畫僅含 {P1} 任務，無跨優先序驗證需求。

## 工作項目

### P1 — 修改
- [x] [P1] T1 修改 `DispatchController.ClearLot`：`HaveFlag "0"→"1"`，`RackId`／`WorkOrder`／`PutTime` 清空行為不動，補註解「清除物料時空平板留置原位，標空板(1)以免回送疊板」
  └─ 非 TDD：SCP 無測試專案、使用者裁定採人工驗證；屬集中單點字面值改動
  └─ 完成：DispatchController.cs:589 `"0"→"1"` + 註解；Gate 1 Build 0 error；commit 248a811、已 push GitHub。Gate 2/4 現場人工驗證由使用者部署時簽核。

- [x] [P1] T2 一廠（OneFactory）同步修改 `DispatchController.ClearLot`：`HaveFlag "0"→"1"`（line 563），其餘不動
  └─ ⚠️ 一廠專屬差異（已查證，不照搬二廠推論）：
     1. 一廠 `releaseAreas=['B','O','P','S','N','G','K','M','I']`，**M 在一廠是 Release 區、不是清除物料區**（與二廠相反）。
     2. 一廠「清除物料」實際出現在 **J/Q/R/T/A/L**（非 Release 區）；line 942 註解「M/J/Q/R」是從二廠複製的舊註解、與實際條件不符。
     3. 一廠 cPair 掃 `HaveFlag='1'` 當來源的只有 Block B/F（BtoA 已註解、line 117），**與清除物料區零重疊 → 無幽靈任務風險**。A 區僅被當 HaveFlag=0 終點，清除→1 只是退出終點池，安全。
     4. 一廠無 O/P 空板回收 binding。
  └─ 完成：OneFactory/.../DispatchController.cs:563 `"0"→"1"` + 註解；一廠 SCP Build 0 error；一廠 repo commit 27d407d、已 push GitHub。Gate 2/4 現場人工驗證由使用者部署時簽核。

## Gate 2／4 人工驗證清單（人類填寫）

> 在測試庫（DESKTOP-2I3FKA2 / agvDB_1400004，**非客戶現場**）操作，驗畢清除測試資料。

- [ ] 測試庫 M 站建立物料 → `HaveFlag=3`
- [ ] 按「清除物料」→ DB 實查該站 `HaveFlag=1`
- [ ] 清除後 `RackId`／`WorkOrder`／`PutTime` 仍為空
- [ ] 看板該格圖示由空架（empty.svg）變空板（trac.svg）
- [ ] O/P 空板回送（Release）不再選中該清除格當落點
- [ ] T/J/Q/R 清除行為與 M 一致（HaveFlag=1）

## Commit 策略
- 格式：`fix(dispatch): <說明>`
- 版本號不變更（無版本號變更任務）。
