# TaskType 資料庫遷移 - 全流程驗證清單

## 一、資料庫驗證
| # | 驗證項目 | 預期結果 | 通過 |
|---|---------|---------|------|
| 1 | 在 SSMS 執行 `SELECT * FROM oTaskTypeRoute WHERE UseFlag='Y'` | 顯示所有啟用路由（Transport 8 筆 + EmptyMove 6 筆） | ✅ |
| 2 | 確認無同 MoveType+FromFloor+ToFloor 重複資料 | `SELECT MoveType, FromFloor, ToFloor, COUNT(*) FROM oTaskTypeRoute WHERE UseFlag='Y' GROUP BY MoveType, FromFloor, ToFloor HAVING COUNT(*) > 1` 應回傳 0 筆 | ✅ |

## 二、搬運任務 TaskType 查詢（與遷移前一致）
| # | 驗證項目 | 預期 TaskType | 通過 |
|---|---------|--------------|------|
| 3 | 1F→1F 同樓層搬運 | F002 | ✅ |
| 4 | 2F→2F 同樓層搬運 | F001 | ✅ |
| 5 | 1F→3F 跨樓層搬運 | F13Test | ✅ |
| 6 | 3F→1F 跨樓層搬運 | F31Test | ✅ |
| 7 | 2F→4F 跨樓層搬運 | F24Test | ✅ |
| 8 | 未定義路線搬運（如 1F→2F） | fallback 使用 config 的 AGVTaskType（F001） | ✅ |

> **驗證方式**：啟動 HikAGVWebAPI 服務，觸發搬運任務，觀察 Log 中 `actualTaskType` 的值，或在 `Dispatch.cs` 的 `GetSchedulingTask` 加斷點檢查。

## 三、EmptyMove 查詢
| # | 驗證項目 | 預期結果 | 通過 |
|---|---------|---------|------|
| 9 | EmptyMove 路線有定義時 | 返回對應 TaskType | ⏳ 待實作 |
| 10 | EmptyMove 路線未定義時 | fallback 使用 AGVTaskType（F001） | ⏳ 待實作 |

> **備註**：EmptyMove 空車任務功能尚未實作，待 AGV自動歸位機制 / 預調度機制完成後補驗。

## 四、管理介面 CRUD
| # | 驗證項目 | 預期結果 | 通過 |
|---|---------|---------|------|
| 11 | 開啟頁面 `/TaskTypeRoute` | 正確顯示所有路由資料 | ✅ |
| 12 | 新增一筆路由 | 資料寫入 DB，頁面 reload 後顯示 | ✅ |
| 13 | 新增重複路由（同 MoveType+FromFloor+ToFloor） | 前端 alert 提示「路由重複」，阻止新增 | ✅ |
| 14 | 編輯一筆路由的 TaskType | 資料更新至 DB | ✅ |
| 15 | 編輯時改為已存在的組合 | 前端 alert 提示「路由重複」 | ✅ |
| 16 | 刪除一筆路由 | 資料從 DB 移除 | ✅ |
| 17 | 取消按鈕 | 所有未儲存的變更還原 | ✅ |

## 五、config 清除確認
| # | 驗證項目 | 預期結果 | 通過 |
|---|---------|---------|------|
| 18 | HikAGV.config 的 `<AGVSettings>` | 不含 `SameFloorTaskTypeMap` 和 `CrossFloorTaskTypeMap` | ✅ |
| 19 | 服務啟動不報錯 | 移除屬性後不影響 config 載入 | ✅ |
